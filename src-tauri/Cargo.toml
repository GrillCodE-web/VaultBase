[package]
name = "cc-manager"
version = "0.1.0"
description = "CC Manager — Desktop CRM for credit card operations"
authors = []
edition = "2021"

[lib]
name = "cc_manager_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "cc-manager"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri                = { version = "2", features = ["devtools"] }
tauri-plugin-dialog  = "2"
tauri-plugin-updater = "2"
rusqlite      = { version = "0.31", features = ["bundled"] }
serde         = { version = "1", features = ["derive"] }
serde_json    = "1"
aes-gcm       = "0.10"
bcrypt        = "0.15"
sha2          = "0.10"
base64        = "0.22"
uuid          = { version = "1", features = ["v4"] }
chrono        = { version = "0.4", features = ["serde"] }
ureq          = { version = "2", features = ["json"] }
regex         = "1"
rand          = "0.8"
once_cell     = "1"
dirs          = "5"
imap          = "2.4"
native-tls    = "0.2"
mailparse     = "0.14"

[features]
custom-protocol = ["tauri/custom-protocol"]
