# Copy to .env and fill in before deploy
# Generate SERVER_SECRET with: openssl rand -hex 32

SERVER_SECRET=REPLACE_WITH_64_RANDOM_HEX_CHARS
ADMIN_USER=admin
ADMIN_PASS=REPLACE_WITH_STRONG_PASSWORD
PORT=3000
# DB_PATH=./data.db   # optional, defaults to ./data.db
