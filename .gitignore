# Dependencies
node_modules/

# Build output
dist/
src-tauri/target/
src-tauri/gen/

# Test coverage
coverage/

# macOS
.DS_Store

# Logs
*.log

# Data files (local development only)
*.db
*.db-shm
*.db-wal
*.db.salt
*.db.plaintext.bak
# REDESIGN-05-6: локальные десктоп-настройки (трей/окна/panic-хоткей)
desktop.json
*.xlsx
*.csv
*.zip

# Claude Code
.claude/

# Agent tool caches (локальные, не коммитим)
.serena/
.abacusai/
.agents/
.codex/

# Release artifacts (бинарники — не в git, заливаются скриптом publish-release.py)
.release-artifacts/

# Playwright test results
test-results/
playwright-report/

# Environment variables
.env
.env.local
.env.*.local

# Ключи подписи апдейтера — приватный ключ НИКОГДА не коммитить.
# Потеря ключа = невозможность выпускать обновления для уже установленных клиентов.
.secrets/
*.key
!*.key.pub

# Локальная Rust-цепочка. НЕ коммитить: файл фиксирует
# stable-x86_64-pc-windows-gnu, и в CI macOS/Linux падали с
# "target tuple in channel name". Каждый разработчик держит свой,
# пример — src-tauri/rust-toolchain.toml.example
src-tauri/rust-toolchain.toml

audit-shots/
audit-run*.log
dev-server.log
audit-shots-before/

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/proxieddb/

# Release bundles (kept on server, not in repo)
.release-artifacts/

# Design mockups (��������� ���������, �� ��� ����)
design-mockups/
