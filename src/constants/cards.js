/**
 * Домен-константы карточного пула (CLEAN-004: магические числа → имена).
 * Здесь только бизнес-пороги, завязанные на правила пула, — не UX-размеры.
 */

/** Карантин: карта считается «новой» первые 14 дней после взятия (P2-QUARANTINE). */
export const QUARANTINE_DAYS = 14

/** Милисекунд в сутках — для пересчёта возраста карты в дни. */
export const MS_PER_DAY = 24 * 60 * 60 * 1000

/** Порог «прогарела» (burn-count): 3+ заказа — medium, 5+ — high. */
export const BURN_COUNT_MEDIUM = 3
export const BURN_COUNT_HIGH = 5

/** Истечение срока карты: меньше 30 дней — скоро, меньше 7 — критично. */
export const EXPIRY_SOON_DAYS = 30
export const EXPIRY_CRITICAL_DAYS = 7

/** Виртуализация таблицы карт включается выше 50 строк (вместо 200) — ради плавности. */
export const CARDS_VIRTUAL_THRESHOLD = 50

/**
 * Окно Undo при удалении карты: тост и отложенное удаление живут одинаково —
 * числа обязаны совпадать, поэтому одно имя.
 */
export const DELETE_UNDO_WINDOW_MS = 5000

// MGR-018 (этап E1): FLASH_HIGHLIGHT_MS выпилена вместе с групповым
// sync'ом (подсветка «живых» обновлений строк больше не применяется).
